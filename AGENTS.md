# TENDERWEIGHT

Standalone original 3D construction and balance rescue game. Never modify PROMPTRON, the fifteen original cartridges, or completed sibling games.
Read docs/CONTRACT.md before implementation. Root owns design, integration, verification and Git. Workers own explicitly assigned files only; preserve others' changes. No descendants, builds, commits, pushes or visible browsers unless explicitly delegated.
Every git push requires its own user approval. Only public kkp8121-rgb repositories may be used. Stage explicit paths only. Never print/commit secrets or auth files.
No start/open/xdg-open or visible browser. Headless Playwright is allowed. Runtime must support file:// and Pages subpaths with bundled classic IIFE, relative assets and no runtime network.
Inspection state must be copied; never expose live state setters. Full play verification uses real inputs; separate isolated unit fixtures from campaign evidence.
Verify geometry, controls, axis signs, visibility and pointer hit-testing through rendered views. A DOM element can be visually obscured despite CSS visibility.
Responsive panel selectors must match actual DOM IDs/classes; verify all visible controls by center hit-testing at 390x844 and 844x390 after changing layout.
Use apply_patch for Korean and nested quoting. Keep generated artwork originals and use format-only WebP conversion. Read the GitHub user id rather than guessing a noreply address.
